apiVersion: v1
kind: Service
metadata:
  name: task-manager
  namespace: prod
spec:
  type: NodePort
  ports:
    - name: https
      port: 3001
      targetPort: 3001
      nodePort: 30000
    # - name: backend
    #   port: 3000
    #   targetPort: 3000
    #   nodePort: 30006
  selector:
    app: task-manager-prod
